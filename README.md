# Streamlit Forum Metrics

A Streamlit hackathon project! 🤺


## Usage

Install requirementas as per usual:

```bash
$ pip install -r requirements.txt
```

Then run the app:

```bash
$ streamlit run streamlit_app.py
```
